---
title: "프로그래머스 Lv.1 - 서울에서 김서방 찾기"
date: 2026-04-07 13:00:00 +0900
categories: ["알고리즘", "프로그래머스"]
tags: ["algorithm"]
render_with_liquid: false
description: "핵심 접근 — 선형 탐색으로 'Kim'의 인덱스를 찾아 문자열로 조립 — O(n)"
image: /assets/img/thumbs/programmers.svg
---

> 출처: <https://school.programmers.co.kr/learn/courses/30/lessons/12919>

```cpp
// 프로그래머스 Lv.1 - 서울에서 김서방 찾기
// https://school.programmers.co.kr/learn/courses/30/lessons/12919
//
// [문제]  String 배열 seoul에서 "Kim"의 위치 x를 찾아 "김서방은 x에 있다"를 반환
// [제약]  seoul 길이 1~1000 / 원소 길이 1~20 / "Kim"은 반드시 한 번 포함
// [입출력]  ["Jane","Kim"] → "김서방은 1에 있다"
//
// 풀이: 선형 탐색으로 "Kim"의 인덱스를 찾아 문자열로 조립 — O(n)

#include <string>
#include <vector>

using namespace std;

string solution(vector<string> seoul) {
    string answer = "";
    for (int i = 0; i < (int)seoul.size(); i++) {
        if (seoul[i] == "Kim") {
            answer = "김서방은 " + to_string(i) + "에 있다";
            break;
        }
    }
    return answer;
}
```

> **오늘 배운 것** — 배열을 앞에서부터 순회하며 `==`로 문자열을 비교하고, 찾는 즉시 `break`하는 선형 탐색 O(n)이면 충분한 크기(길이 최대 1000)의 문제다.
{: .prompt-tip }

> **면접에서 이렇게 말한다** — 예상 질문: "선형 탐색은 언제 쓰고 시간복잡도는 어떻게 되나요?" → O(n), 정렬 불필요, 조기 종료(break), 작은 입력 크기
{: .prompt-info }
