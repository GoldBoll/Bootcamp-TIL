---
title: "[TIL] 2026-03-25 — 팀프로젝트 콘솔게임 발표 자료 (기획·시스템)"
date: 2026-03-25 09:00:00 +0900
categories: ["TIL"]
tags: ["daily", "til"]
render_with_liquid: false
image: /assets/img/thumbs/til.svg
description: "콘솔 로그라이크 '던전노조' 팀프로젝트 발표 자료 정리 — BSP 맵 자동 생성 중심"
---

3/25부터 4/1까지 진행하는 콘솔게임 팀프로젝트의 발표 자료를 정리했다. 팀은 팀장인 나(신장식)와 김지원·이승진·전종환, 4명이다. 만드는 게임은 **던전노조(가제)** — 중세 판타지 배경의 로그라이크 턴제 던전 탐험 RPG로, 콘솔(터미널) 화면 위에서 100×100 타일 던전을 내려가 마지막 층의 보스를 잡는 것이 목표다. 레퍼런스는 Shattered Pixel Dungeon.

![팀 소개](/assets/img/til/2026-03-25/image.png)

![레퍼런스 — Shattered Pixel Dungeon](/assets/img/til/2026-03-25/image-1.png)

발표 자료에서 비중이 가장 큰 부분은 BSP 맵 자동 생성이라, 이 글도 그 순서로 정리한다. 기획 규칙을 먼저 훑고, 시스템 구성 → BSP → 나머지 시스템(렌더·아이템·스폰·몬스터) 순이다.

기획 문서는 따로 모아 두었다: [스프레드시트](https://docs.google.com/spreadsheets/d/1cM5d1gMhKLpuqJOGj-DiciZgW4YiUPYYDTq7VYwd27o/edit?usp=sharing), [플로우차트.excalidraw](https://www.notion.so/excalidraw-1-335f77b24d2f80d494ace395e0971d21?pvs=21)

## 기획 요약 — 규칙과 수치

이동은 상·하·좌·우 1칸씩만 가능하고 대각선 이동은 없다. 입력은 방향키 또는 W/A/S/D. 턴 기반이라 플레이어가 1칸 이동하면 몬스터도 1회 행동하며, 대쉬를 켜면 한 칸 더 이동한다.

몬스터와 인접(접점)하면 전투가 발생하고, 전투 중에는 `공격` / `아이템 사용` / `이동(도망)` 중에서 고른다. 명중률은 무기별로 다르다. 회복 수단은 몬스터 처치 시 드롭되는 아이템과 레벨업 이벤트 회복(선택 사항)이다.

레벨업마다 아래 보너스 중 1가지를 선택한다 (레벨 구간 LV.1~LV.15, 레벨업 요구 경험치 20):

- 최대 체력 증가 +3
- 공격력 증가 +1
- 방어력 증가 +1
- 민첩 증가 +1
- 현재 체력 회복 +5

## 맵과 타일

전체 맵은 100×100 타일이고, 각 층은 방 탐색 구조다 — 방마다 구조물(벽), 몬스터, 아이템을 배치한다. 다음 층 계단(`>`)은 방 리스트의 마지막 방에 생성한다.

타일 범례는 기획서와 구현 문서에 나뉘어 있던 것을 하나로 합쳤다.

| 기호 | 의미 | 10 이내면 생성 X |
| --- | --- | --- |
| `#` | 벽 (통과 불가) |  |
| `@` | 플레이어 위치 | X |
| `R` | 바위 (좁은 장애물) |  |
| `W` | 물 (넓은 장애물) |  |
| `M` | 일반 몬스터 |  |
| `E` | 엘리트 몬스터 |  |
| `B` | 보스 |  |
| `>` | 계단 (다음 층) | X |
| `C` | 보물 상자 |  |
| (공백) | 빈 공간 (이동 가능) — 투명 처리, 여기서만 생성 |  |

## 시스템 구성

코드는 매니저 단위로 나눴다.

| 시스템 | 파일 | 핵심 역할 |
| --- | --- | --- |
| **GameManager** | `GameManager/GameManager.h·cpp` | 게임 루프, 입력→행동→렌더 총괄 |
| **InputManager** | `GameManager/InputManager.h·cpp` | 키 입력 추상화 (GameAction) |
| **BspManager** | `BspManager/BspManager.h·cpp` | BSP 알고리즘 맵 자동 생성 |
| **ItemManager** | `Item/ItemManager.h·cpp` | 아이템 생성 팩토리 + 인벤토리 UI |
| **Render** | `Render/Render.h·cpp` | 콘솔 렌더링 시스템 전체 |
| **SpwanManager** | `SpawnManager/SpawnManger.h.cpp` | 스폰 관련 시스템 |

게임 플로우차트는 그림으로 정리했다. ([드라이브 원본](https://drive.google.com/file/d/1l0a_tAmKJ3JVNhTSTlvVcLZJA4u_7FsF/view?usp=sharing))

![게임 플로우차트](/assets/img/til/2026-03-25/image-2.png)

## 게임 루프와 입력

GameManager가 입력→행동→렌더 순서의 게임 루프를 총괄하고, InputManager는 키 입력을 GameAction으로 추상화해서 넘긴다.

![핵심 멤버 구조](/assets/img/til/2026-03-25/image-3.png)

![게임 루프 흐름](/assets/img/til/2026-03-25/image-4.png)

![게임 실행 화면 전체](/assets/img/til/2026-03-25/image-5.png)

키 바인딩은 다음과 같다.

| 키 | 행동 |
| --- | --- |
| W / S / A / D 및 방향키 | 이동 (상/하/좌/우) |
| 1 | 공격 (전투 중) |
| 2 | 도망 (전투 중) |
| ` (백틱) | 대쉬 활성화 |
| i | 인벤토리 열기/닫기 |
| Space | 아이템 사용 |
| h | 도움말 |
| q | 종료 |

## 맵 자동 생성 — BSP 알고리즘

BSP(Binary Space Partitioning)는 공간을 재귀적으로 **두 영역으로 계속 분할**하는 알고리즘이다.

```
[전체 맵 공간]
        │
   ┌────┴────┐          ← 좌/우 (또는 상/하) 랜덤 분할
   │         │
 ┌─┴─┐     ┌─┴─┐        ← 다시 분할
 │   │     │   │
[방] [방] [방] [방]     ← 더 이상 분할 불가 → 각 영역에 방 하나씩 생성
```

전체 흐름은 다섯 단계다.

1. **분할** — 공간이 최소 크기보다 크면 랜덤 방향(가로/세로)으로 절반씩 분할
2. **방 생성** — 더 이상 분할할 수 없는 말단 영역(leaf)마다 랜덤 크기의 방 조각
3. **복도 연결** — 트리를 거슬러 올라가며 형제 노드의 방 중심을 Z자형 복도로 연결
4. **복도 확장** — 1칸 너비 복도를 4방향 팽창시켜 3칸 너비로 확장
5. **방 타입 배정** — 첫 방 = Start, 마지막 방 = Stair, 나머지는 랜덤으로 Elite/Treasure/Normal 배정

코드에서는 이 흐름이 하나의 생성 파이프라인으로 이어진다.

```
Generate(map, params)
  │
  ├─ 1. map.Fill(Wall)          ← 전체 Wall로 초기화
  ├─ 2. BuildTree()             ← 파티션 재귀 분할 (BSP 트리 생성)
  ├─ 3. CarveRooms()            ← leaf 노드마다 랜덤 크기 방을 Floor로 조각
  ├─ 4. BuildRoomSnapshot()     ← 방 타일 스냅샷 저장 (복도/방 구분용)
  ├─ 5. ConnectTree()           ← Z자형 복도 중심선(1칸) 연결
  ├─ 6. DilateCorridor()        ← 복도를 4방향 팽창 → 3칸 너비로 확장
  ├─ 7. MarkDebugRoomWalls()    ← 방 경계 벽 색상 구분용 마킹
  └─ 8. AssignRoomTypes()       ← 방 타입 랜덤 배정
```

### 복도 구현 로직 상세

**1단계 — HCorridor / VCorridor (복도 중심선 그리기)**

```cpp
// 수평 복도: 지정한 행(row)에서 startCol ~ endCol 구간을 Floor으로 채움
void HCorridor(Map& map, int startCol, int endCol, int row)
{
    for (int col = min(startCol,endCol); col <= max(startCol,endCol); ++col)
        map.SetTile(col, row, Tile::Floor);
}

// 수직 복도: 지정한 열(col)에서 startRow ~ endRow 구간을 Floor으로 채움
void VCorridor(Map& map, int col, int startRow, int endRow)
{
    for (int row = min(startRow,endRow); row <= max(startRow,endRow); ++row)
        map.SetTile(col, row, Tile::Floor);
}
```

**2단계 — ConnectTree (Z자형 복도 연결)**

모든 연결은 **H복도 → V복도 → H복도** (Z자형) 또는 반대 순서로 이어진다.

```
[수직 분할: 좌||우]

  좌방 중심  spine열  우방 중심
      *──────── |           ← H복도
                |
                |───────*  ← V복도 → H복도

[수평 분할: 상=하]

  위방 중심
      *
      |             ← V복도
      |────────*  ← H복도
                |
                *   ← V복도
  아래방 중심
```

**3단계 — DilateCorridor (1칸 → 3칸 확장)**

```
 [확장 전]     [확장 후]

  # # # #      # # # #
  #   # #      #     #
  # # # #  →   #     #
  # # # #      #     #
  # # # #      # # # #

  중심선 1칸    주도 3칸
```

복도 중심선(Floor이면서 방이 아닌 타일)을 먼저 탐지한 뒤, 해당 타일의 4방향 좌표를 전부 수집해 두고 일괄로 Floor로 변환한다. 순회하면서 바로 타일을 바꾸면 방금 넓힌 칸이 다시 중심선으로 잡힐 수 있어서, 수집과 변환을 분리하는 방식이다.

![BSP 맵 생성 결과](/assets/img/til/2026-03-25/bsp.gif)

## 렌더링과 UI

콘솔 렌더링은 Render 시스템이 전담한다. 화면은 맵 뷰포트 + 플레이어 정보 + 로그의 3분할 레이아웃이다.

```
┌────────────────────────┬──────────┬────────────────┐
│                        │  Player  │                │
│          Map           │   Info   │      Log       │
│      (뷰포트 기반)      │──────────│   (게임 로그)  │
│                        │  Info 2  │                │
├────────────────────────┴──────────┴────────────────┤
│  [help:h]  [inven:i]  [move:WASD]  [quit:q]        │
└────────────────────────────────────────────────────┘
```

![기본 배경 UI](/assets/img/til/2026-03-25/image-6.png)

![UI 예시 1](/assets/img/til/2026-03-25/image-7.png)

![UI 예시 2](/assets/img/til/2026-03-25/image-8.png)

![전체 UI 레이아웃 화면](/assets/img/til/2026-03-25/image-9.png)

![RenderInfo HP바](/assets/img/til/2026-03-25/image-10.png)

## 아이템 시스템

![아이템 시스템](/assets/img/til/2026-03-25/image-11.png)

| 코드 | 이름 | 효과 | 사용 가능 상황 |
| --- | --- | --- | --- |
| 101 | 회복 물약 | 체력 10 회복 | 전투 / 비전투 |
| 102 | 랜덤 이동 물약 | 맵 내 랜덤 Floor로 순간이동 | 비전투 중 |
| 103 | 투명화 아이템 | 5턴 동안 몬스터 탐지 회피 | 비전투 중 |
| 105 | 부활 토큰 | 사망 시 최대체력 50%로 자동 부활 | 전투 / 비전투 |

![인벤토리 UI 화면](/assets/img/til/2026-03-25/image-12.png)

![아이템 획득 → 인벤토리 → 사용 흐름](/assets/img/til/2026-03-25/image-13.png)

## 스폰 시스템

스폰은 SpawnManager가 싱글턴으로 담당하고, 스테이지별로 등장 몬스터 구성이 다르다(Stage1~Stage4).

| 이름 | 스폰 위치 |
| --- | --- |
| 플레이어 | 첫번째 방 |
| 계단 | 마지막 방 |
| 상자 | 랜덤 |
| 몬스터 | 첫번째 방과 마지막을 제외한 방 |
| 엘리트 몬스터 | 엘리트방, 상자방 |
| 보스 | 마지막 스테이지 |

```cpp
class SpawnManager
{
private:
    SpawnManager();
    SpawnManager(const SpawnManager&) = delete;
    SpawnManager& operator=(const SpawnManager&) = delete;

    std::vector<Monster*> activeMonsters;
    int stage;

public:
    static SpawnManager* GetInstance()
    {
        static SpawnManager instance;
        return &instance;
    }
    ~SpawnManager();

    const std::vector<Monster*>& GetActiveMonsters() const; // 저장 및 조회
    void SpawnMonstersInRooms(Map& _map, const std::vector<Room>& _rooms, int _stage);
    void UpdateCleanup(Map& _map);
    void SpawnPlayerInRooms(Map& _map, const std::vector<Room>& _roomList, Player* player);
    void SpawnObjectInRooms(Map& _map, const std::vector<Room>& _roomList);
    Monster* GetMonsterAt(int targetX, int targetY);

		// 스테이지 별 등장 몬스터 구성이 다름
    void Stage1(Map& _map, const std::vector<Room>& _rooms);
    void Stage2(Map& _map, const std::vector<Room>& _rooms);
    void Stage3(Map& _map, const std::vector<Room>& _rooms);
    void Stage4(Map& _map, const std::vector<Room>& _rooms);
};
```

![스폰 결과 화면](/assets/img/til/2026-03-25/image-14.png)

## 몬스터 시스템

일반 몬스터 4종과 각각의 엘리트 버전, 보스 1종이 있다. 미믹은 상자로 위장한다.

| 스탯 | 고블린 | 오크 | 미믹 | 골렘 |
| --- | --- | --- | --- | --- |
| 최대 체력 | 10 | 15 | 20 | 50 |
| 공격력 | 3 | 5 | 7 | 9 |
| 방어력 | 0 | 4 | 7 | 10 |
| 민첩 | 3 | 3 | 5 | 5 |
| 경험치 | 5 | 5 | 7 | 7 |

| 스탯 | 엘리트_고블린 | 엘리트_오크 | 엘리트_미믹 | 엘리트_골렘 | 강철_골렘 (보스) |
| --- | --- | --- | --- | --- | --- |
| 최대 체력 | 15 | 22 | 30 | 60 | 200 |
| 공격력 | 5 | 8 | 10 | 13 | 20 |
| 방어력 | 0 | 6 | 10 | 14 | 20 |
| 민첩 | 4 | 4 | 7 | 7 | 10 |
| 경험치 | 8 | 8 | 10 | 10 | 100 |

몬스터 이동은 턴제 규칙을 그대로 따른다. 플레이어가 1턴을 쓰면 각 몬스터도 1턴을 받아 움직이는데, IDLE 상태일 때만 30% 확률로 랜덤 방향 이동을 시도한다.

```cpp
void Monster::Move(int _dx, int _dy, Map& _map)
{
    int nextX = x + _dx;
    int nextY = y + _dy;

    // 현재 위치와 다음 위치가 모두 유효해야 동작
    if (_map.InBounds(nextX, nextY) && _map.GetTile(nextX, nextY) == Tile::Floor)
    {
        // 이전 위치 타일 정리
        _map.SetTile(x, y, Tile::Floor);

        x = nextX;
        y = nextY;

        // 자신의 상태(Elite 여부)에 맞는 타일 배치
        if (isElite)
            _map.SetTile(x, y, Tile::EliteMonster);
        else
            _map.SetTile(x, y, Tile::Monster);
    }
}

void Monster::UpdateAI(Map& _map)
{
    // IDLE 상태일 때만 랜덤 이동
    if (state != MonsterState::IDLE) return;

    // 30% 확률로 이동
    if (rand() % 100 > 30) return;

    int direction = rand() % 4;
    int dx = 0, dy = 0;

    switch (direction)
    {
    case 0: dy = -1; break; // 위
    case 1: dy = 1;  break; // 아래
    case 2: dx = -1; break; // 왼쪽
    case 3: dx = 1;  break; // 오른쪽
    }

    if (_map.GetTile(x + dx, y + dy) == Tile::Floor)
        this->Move(dx, dy, _map);
}
```

![몬스터 랜덤 이동 화면](/assets/img/til/2026-03-25/no-title.gif)

> **오늘 배운 것** — BSP 던전 생성은 공간을 재귀 분할해 leaf마다 방을 만들고, 트리를 거슬러 올라가며 형제 노드의 방 중심을 Z자형 복도(H→V→H)로 잇는 구조다. 1칸 복도를 3칸으로 넓힐 때는 팽창 대상 좌표를 먼저 전부 수집한 뒤 일괄 변환해야 순회 중 오염이 없다.
{: .prompt-tip }

> **면접에서 이렇게 말한다** — 예상 질문: "로그라이크 던전 맵을 절차적으로 생성한다면 어떻게 구현하시겠어요?" → BSP 재귀 분할, leaf 방 생성, 형제 노드 복도 연결, Z자형 복도, 복도 팽창(Dilate)
{: .prompt-info }
