---
title: "[TIL] 2026-03-12 — 코딩테스트 강의 정리(챕터 3-7)"
date: 2026-03-12 09:00:00 +0900
categories: ["TIL"]
tags: ["daily", "til"]
render_with_liquid: false
image: /assets/img/thumbs/til.svg
---

오늘은 코딩테스트 강의 챕터 3-7(트리)을 들었다. 트리의 계층 구조와 용어(루트/부모/자식/리프)를 잡고, 표현 방식과 순회를 문제 유형과 연결해 정리했다.

## 트리 개념 및 탐색

트리는 비선형 자료구조로 계층적 데이터를 표현한다. 순회는 세 가지를 구분한다 — 전위는 루트-왼-오, 중위는 왼-루트-오, 후위는 왼-오-루트 순서다. 처음에는 순서를 그냥 외우려고 했는데, "이 순회가 어떤 문제에 쓰이는가"와 연결하니 훨씬 정리가 쉬웠다. 특히 중위 순회는 BST(이진 탐색 트리)에서 정렬된 순서를 뽑아내는 것과 직결된다는 점이 와닿았다.

구현 관점에서는 두 가지 표현을 비교했다. 배열 기반은 완전이진트리 형태에서 인덱스 계산이 단순하고, 인접리스트 기반은 일반 트리나 희소 구조에 유연하다.

```cpp
vector<vector<int>> tree(n+1);
tree[parent].push_back(child);
```

BST는 현재 노드를 기준으로 작은 값은 왼쪽, 큰(또는 크거나 같은) 값은 오른쪽에 배치하는 규칙으로 동작한다. 균형이 잡혀 있으면 탐색/삽입/삭제가 빠르지만, 한쪽으로 편향되면 성능이 리스트 수준까지 떨어질 수 있다.

| 연산 | 평균 | 최악 |
| --- | --- | --- |
| 탐색 | O(log N) | O(N) |
| 삽입 | O(log N) | O(N) |
| 삭제 | O(log N) | O(N) |

복습으로 할 것:

- [ ]  전위/중위/후위 순회 템플릿 손코딩 1회
- [ ]  BST 삽입+탐색 미니 문제 1개 풀이

> **오늘 배운 것** — 트리 순회는 순서 암기가 아니라 문제 목적과 연결해서 이해해야 한다. 중위 순회가 BST의 정렬 순서 추출과 직결된다는 것, 그리고 BST는 편향되면 최악 O(N)까지 떨어진다는 것이 핵심이다.
{: .prompt-tip }

> **면접에서 이렇게 말한다** — 예상 질문: "BST의 탐색이 O(log N)이 아닐 수 있는 경우는 언제인가요?" → 편향 트리, 최악 O(N), 균형 여부, 중위 순회 = 정렬 순서, 평균 O(log N)
{: .prompt-info }
